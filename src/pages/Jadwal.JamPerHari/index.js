import ListProvider from "components/list";
import Layout from "./Layout";

const JamPerHari = () => {
  return (
    <ListProvider>
      <Layout />
    </ListProvider>
  )
}

export default JamPerHari;