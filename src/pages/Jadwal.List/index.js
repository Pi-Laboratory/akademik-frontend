import ListProvider from "components/list";
import Layout from "./Layout";

const JadwalList = () => {
  return (
    <ListProvider>
      <Layout />
    </ListProvider>
  )
}

export default JadwalList;