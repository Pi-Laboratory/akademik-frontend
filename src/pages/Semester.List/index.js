import ListProvider from "components/list";
import Layout from "./Layout";

const ListSemester = () => {
  return (
    <ListProvider>
      <Layout />
    </ListProvider>
  )
}

export default ListSemester;